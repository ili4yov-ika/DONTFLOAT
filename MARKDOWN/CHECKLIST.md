- [x] Сейчас кнопки воспроизведения (Play или Stop) в плагине DONTFLOAT Pitcher
      — `requestHostTransport()` обязан переопределять каждый редактор, который
        бывает содержимым окна: база возвращает false. В Pitcher содержимое —
        пианорольный редактор, и он запрос никуда не передавал. В полной редакции
        того же не было: она пробрасывает запрос в волновую половину.
      — закреплено проверкой `testTransportButtonsReachTheHost` (цель собрана
        именно как Pitcher).
- [ ] Если на первую дорожку добавить DONTFLOAT, а на другую DONTFLOAT Pitcher, то в DONTFLOAT почему-то редактироваться референсные ноты от второй дорожки (они должны серыми, а синими)
      — синими ноты становятся ровно в одном месте: когда попали в рабочий набор
        из источника, который экземпляр посчитал своим. Поиск своей дорожки
        переделан (свои клипы важнее выбора в DAW, несколько region sequences —
        это неопределённость, а не «первая попавшаяся»).
      — механизм закреплён тестом `plugin_reference_notes_test`: два настоящих
        пианоролла на одном документе ARA, октава между дорожками, наборы не
        смешиваются.
      — проверять после переустановки плагинов. В дневнике всё видно:
        `ara.notes.own source=…` (чей источник посчитан своим) и
        `ara.notes.reference from=… for=…` (чьи ноты отданы соседу и кому).
        Если у обоих экземпляров `source=` одинаковый — на дорожках один и тот же
        файл, и для ARA это **один** источник: своих нот у соседа тогда нет
        по построению, и разделить их нельзя, не заводя разметку на уровне
        audio modification.
- [x] Почини горизонтальный скролл в плагине DONTFLOAT и добавь горизонтальный скролл в DONTFLOAT Pitcher
      — полоса под волной заводилась с нулевым максимумом и такой оставалась:
        диапазон никто не пересчитывал, тянуть бегунок было некуда.
      — в Pitcher не было ни полосы, ни приближения: колесо над пианороллом
        только просило волну изменить масштаб, а волны в этой редакции нет.
        Теперь пианоролл считает масштаб сам, а полная редакция сообщает ему,
        что рядом есть волна и масштаб её.
      — закреплено проверками `testWaveformScrollBarFollowsZoomAndMovesTheView`
        и `testPianoRollZoomsAndScrollsOnItsOwn`.
      — приближение в пианоролле — `Ctrl`+колесо (просто колесо листает по высоте).
- [x] Питчер не работает: ноты можно двигать, но при общем воспроизведении изменения не слышны
      — под ARA выход плагина хост не слушает вовсе: дорожку выдаёт наш рендерер,
        а он играл исходник. Результат правок теперь уходит в модель
        (`publishEditedAudio`), и рендерер играет его.
      — закреплено проверкой `testHostPlaysEditedAudioNotTheSource`: правка взята
        предельно различимой — тишина вместо звука.
      — правки нот попадают туда по кнопке **«Применить»** на панели пианоролла:
        само перетаскивание ноты звук не пересчитывает.
- [x] В полной редакции плагина DONTFLOAT, при воспроизведении каретки дёргаются вправо-влево, при клике без воспроизведения каретки не сихранизирует положение между собой
      — рывки: позиции из `process()` приходят не по порядку (опережающая обработка
      REAPER), теперь мелкий шаг назад отбрасывается; клик: половины ведут каретку
      друг друга и просят DAW встать туда же (`plugin_transport_sync_test`)
- [x] При разрыве клипов перерисовываются звуковые пики, также проверь поведение питч-сетки при действиях с клипами
      — звук заливается один раз, дальше перечитывается только размещение клипа и
      сетка (`syncAraClipAndGrid`, `testClipEditsDoNotRedrawTheWaveform`); питч-сетка
      считается по клипу самого экземпляра, а не по первому клипу источника
- [x] Подготовь установщики (Windows, Linux (через WSL или Github Action), MacOS (через Github Action)) и плагины для релиза и сделай релиз на Github
      — Windows: установщик и переносимый архив собраны и залиты в черновик
        релиза v0.1.0.0, описание переписано под один формат.
      — Linux: бандл VST3 собирается и лежит в пакете (`/usr/lib/vst3`) — проверено
        распаковкой готового `.deb`.
      — macOS: плагин собирается в CI, но в `.pkg`/`.dmg` не кладётся — бандлу
        нужен свой развёрнутый Qt, и без настоящего Mac проверить это негде.
      — Релиз остаётся **черновиком**: публикация за вами.

## Что нужно сделать руками

1. Переустановить плагины — и REAPER, и тест гоняют **установленные** сборки:
   `tools\build_windows_installer.bat`, затем `tools\install_plugins_windows.ps1`
   (просит права администратора).
2. Прогнать `ctest -C Release -R reaper_ara_integration_test --output-on-failure`
   и посмотреть в дневнике `ara.host.playback=`, `ara.notes.own`,
   `ara.notes.reference`, `ara.audio.edited`.
