- [x] Внедри поддержку ARA 2 в minidaw и плагины SDK подключён (`cmake/AraSdk.cmake`); слой плагина `plugins/ara/` — document controller, разбор нот поверх ARA-доступа к сэмплам, выдача нот хосту (`kARAContentTypeNotes`), темп и размер такта из musical context, разметка в архивах проекта. Привязка к форматам: CLAP (`clap_ara_factory_t`, `clap_ara_plugin_extension_t`) и VST3 (`IMainFactory`, `IPlugInEntryPoint`/`2`, класс фабрики `ARA Main Factory Class`). Мини-DAW работает как ARA-хост (`tools/mini_daw/mini_daw_ara_host.*`): документ, musical context, region sequence, audio source/modification, playback region; `--selftest` проходит весь путь и печатает число нот, полученных через ARA.
- [x] Плагины должны адекватно отрабатывать механизм UNDO/REDO, разделения, перемещения, сжатия/разжатия клипов, при добавлении нового клипа на дорожку плагины должны делать анализ участка с ним ARA: новый клип и новый источник ставятся в разбор сами (`didAddPlaybackRegionToRegionSequence`, `didAddAudioSourceToDocument`); разрез, перенос, обрезка и растяжение приходят как `didUpdatePlaybackRegionProperties` — разбор не перезапускается, ноты клипа отдаются в новых координатах (`doCreatePlaybackRegionContentReader` учитывает смещение, границы куска источника и коэффициент растяжения), фабрика объявляет поддержку timestretch. UNDO/REDO: у хоста — через архивы ARA (проверено round-trip тестом), внутри плагина — свой `QUndoStack` (правка высоты и разрез нот, `Ctrl+Z` / `Ctrl+Y` только при фокусе в редакторе).
- [x] Сделай если на одной дорожке в DAW висит плагин DONTFLOAT или DONTFLOAT Pitcher, то на другой ноты (и следовательно тональности) из первой будут референсными используя механизм ARA 2 Модель: один document controller обслуживает все дорожки документа, `referenceNotesExcluding()` отдаёт ноты соседнего источника (тест заводит две «дорожки», разбирает обе и читает ноты каждой). Интерфейс: экземпляр находит свой источник через роли ARA (`AraPlaybackRenderer` / `AraEditorView`, `audioSourceForInstance`), редактор под ARA берёт из модели свои ноты, тактовую сетку хоста и ноты соседней дорожки серым референсом (`DontfloatPitchEditor::pullFromAraModel`); без ARA остаётся прежний путь через `SharedNoteBoard`. Привязку пробрасывают обёртки CLAP и VST3, состояние окна сообщается ARA (`setEditorOpenState`).
- [x] (дубль пункта выше) Плагины должны адекватно отрабатывать механизм UNDO/REDO, разделения, перемещения, сжатия/разжатия клипов, при добавлении нового клипа на дорожку плагины должны делать анализ участка с ним
- [x] Используй `horizontal-lock.svg` `horizontal-unlock.svg` `vertical-lock.svg` `vertical-unlock.svg`, сделай две кнопки: блокировка/разблокировка перемещения нот горизонтале (по умолчанию заблокирован), блокировка/разблокировка перемещения нот вертикале (по умолчанию разблокирован) Две кнопки-замка на панели пианоролла (`PianoRollToolbar`), иконка меняется по состоянию. Горизонталь закрыта по умолчанию, вертикаль открыта; состояние сохраняется в `QSettings`. Открытая горизонталь двигает ноту по таймлайну с сохранением длины (снап по режиму реза), сдвиг идёт через `PitchNoteMoveCommand` — отменяется `Ctrl+Z`. Закрытая вертикаль запрещает менять высоту перетаскиванием. Курсор над нотой показывает, какие направления открыты.
