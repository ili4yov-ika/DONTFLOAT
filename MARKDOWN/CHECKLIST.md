- [ ] Если на первую дорожку добавить DONTFLOAT, а на другую DONTFLOAT Pitcher, то в DONTFLOAT почему-то редактироваться референсные ноты от второй дорожки (они должны серыми, а синими, а ноты своей дорожки синими, а не серыми)
      — синими ноты становятся ровно в одном месте: когда попали в рабочий набор
        из источника, который экземпляр посчитал своим. Поиск своей дорожки
        переделан (свои клипы важнее выбора в DAW, несколько region sequences —
        это неопределённость, а не «первая попавшаяся»).
      — механизм закреплён тестом `plugin_reference_notes_test`: два настоящих
        пианоролла на одном документе ARA, октава между дорожками, наборы не
        смешиваются.
      — **найдено в дневнике настоящего REAPER**: первый открывшийся редактор
        забирал звук и ноты **соседней** дорожки. Роли хост раздаёт позже
        привязки, а редактор в такой момент брал единственный источник
        документа — в начале сеанса он там и правда один, только чужой.
        Запасного пути больше нет: редактор ждёт, пока хост назначит клип,
        а если источник сменился — забирает звук и разметку заново.
      — закреплено проверкой `testEditorWithoutAClipTakesNobodysNotes`
        (на прежнем коде она красная).
      — проверять после переустановки плагинов.
      — в дневнике всё видно:
        `ara.notes.own source=…` (чей источник посчитан своим) и
        `ara.notes.reference from=… for=…` (чьи ноты отданы соседу и кому).
        Если у обоих экземпляров `source=` одинаковый — на дорожках один и тот же
        файл, и для ARA это **один** источник: своих нот у соседа тогда нет
        по построению, и разделить их нельзя, не заводя разметку на уровне
        audio modification.
- [ ] Питчер не работает: ноты можно двигать, но при общем воспроизведении изменения не слышны
      — тем же и объясняется: положить результат было некуда — источник не
        найден, а публикация в никого молча ничего не делает. Чинится тем же
        отказом от «запасного пути».
      — под ARA выход плагина хост не слушает вовсе: дорожку выдаёт наш рендерер,
        а он играл исходник. Результат правок теперь уходит в модель
        (`publishEditedAudio`), и рендерер играет его.
      — закреплено проверкой `testHostPlaysEditedAudioNotTheSource`: правка взята
        предельно различимой — тишина вместо звука.
      — правки нот попадают туда по кнопке **«Применить»** на панели пианоролла:
        само перетаскивание ноты звук не пересчитывает.
- [ ] В плагинах при воспроизведении каретка или каретки двигается рывками вправо-влево
      — найдена и исправлена одна верная причина: позиция от хоста приходила в
        сэмплах **проекта**, а редактор делил её на частоту **файла дорожки**.
        Файл 44,1 кГц в проекте 48 кГц — почти 9% мимо. Обмен переведён на
        секунды проекта.
      — **вторая причина найдена по дневнику REAPER**: позиции шли всем
        открытым окнам подряд, а экземпляров в проекте несколько (две
        дорожки, а после разреза клипа — два на одной). В дневнике видно два
        потока позиций в одном фильтре. Теперь окно слушает только свой
        экземпляр: адресом служит привязка ARA, она у каждого своя.
      — попутно: «последняя привязка модуля» была одна на всех, и второй
        экземпляр затирал первую — оба окна уезжали на одну дорожку. Теперь
        окно при открытии занимает свободную привязку.
      — проверено сборкой и всем набором тестов (33/33, Linux тоже), но
        **в REAPER не проверено**: установить плагины не дали права.
- [x] При разрыве клипов перерисовываются звуковые пики, также проверь поведение питч-сетки при действиях с клипами
      — звук заливается один раз, дальше перечитывается только размещение клипа и
      сетка (`syncAraClipAndGrid`, `testClipEditsDoNotRedrawTheWaveform`); питч-сетка
      считается по клипу самого экземпляра, а не по первому клипу источника
- [x] Подготовь установщики (Windows, Linux (через WSL или Github Action), MacOS (через Github Action)) и плагины для релиза и сделай релиз на Github
      — Windows: установщик и переносимый архив собраны и залиты в черновик
        релиза v0.1.0.0, описание переписано под один формат.
      — Linux: бандл VST3 собирается и лежит в пакете (`/usr/lib/vst3`) — проверено
        распаковкой готового `.deb`.
      — macOS: плагин собирается в CI, но в `.pkg`/`.dmg` не кладётся — бандлу
        нужен свой развёрнутый Qt, и без настоящего Mac проверить это негде.
      — Релиз остаётся **черновиком**: публикация за вами.

## Что нужно сделать руками

1. **Переустановить плагины — обязательно.** Сейчас в системе лежит сборка
   неудачного опыта со связкой через `IConnectionPoint`: под ней ARA-рендерер
   остаётся без клипов, а REAPER подвисает при выгрузке. Готовый payload с
   текущим кодом уже лежит в `build\install`, поэтому достаточно второй
   команды:
   `tools\install_plugins_windows.ps1` (просит права администратора).
   Пересобрать payload заново — `tools\build_windows_installer.bat`.
2. Прогнать `ctest -C Release -R reaper_ara_integration_test --output-on-failure`
   и посмотреть в дневнике `ara.host.playback=`, `ara.notes.own`,
   `ara.notes.reference`, `ara.audio.edited`.
