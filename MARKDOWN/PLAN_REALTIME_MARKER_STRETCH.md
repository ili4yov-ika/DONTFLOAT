# План реализации реального времени обработки меток сжатия/растяжения

> **Статус**: ✅ **РЕАЛИЗОВАНО** (2024)
> 
> Все основные этапы реализации завершены. Метки сжатия/растяжения работают в реальном времени:
> - Визуально изменяют волну при перетаскивании
> - Обновляют воспроизведение при отпускании метки
> - Корректно работают с системой undo/redo

## Цель

Реализовать обработку аудио в реальном времени при перетаскивании меток сжатия/растяжения с тонкомпенсацией и визуальным отображением измененной волны между метками.

## Текущее состояние

### ✅ Полностью реализовано:
- ✅ Создание и перетаскивание меток
- ✅ Визуальное отображение коэффициентов между метками
- ✅ Обработка аудио с тонкомпенсацией через `applyTimeStretchToMarkers()` (при Ctrl+T)
- ✅ Система отмены/повтора через `TimeStretchCommand`
- ✅ **Обработка аудио в реальном времени при перетаскивании меток**
- ✅ **Визуальное отображение измененной волны между метками в реальном времени**
- ✅ **Обновление воспроизведения при отпускании метки**

### Реализованные компоненты:
1. **Кэширование исходных данных**: `originalAudioData` хранит исходные данные для пересчета
2. **Обработка в реальном времени**: `processRealtimeStretch()` обрабатывает аудио при перетаскивании
3. **Throttling**: `scheduleRealtimeProcess()` ограничивает частоту обработки до 50ms
4. **Обновление воспроизведения**: `updatePlaybackAfterMarkerDrag()` обновляет QMediaPlayer при отпускании метки
5. **Синхронизация с undo/redo**: `TimeStretchCommand` корректно работает с `originalAudioData`

## Архитектура решения

### 1. Кэширование исходных данных

**Проблема**: Нужно всегда иметь доступ к исходным аудиоданным для корректного пересчета при изменении меток.

**Решение**:
- Добавить в `WaveformView` поле `QVector<QVector<float>> originalAudioData` для хранения исходных данных
- При загрузке аудио сохранять копию в `originalAudioData`
- При применении эффекта (Ctrl+T) обновлять `originalAudioData` на новые обработанные данные

```cpp
// В waveformview.h
QVector<QVector<float>> originalAudioData; // Исходные данные для пересчета

// В setAudioData()
originalAudioData = data; // Сохраняем исходные данные
```

### 2. Обработка в реальном времени при перетаскивании

**Место реализации**: `WaveformView::mouseMoveEvent()` при перетаскивании метки

**Алгоритм**:
1. При изменении позиции метки вычислять затронутые сегменты
2. Обрабатывать только измененные сегменты (оптимизация)
3. Обновлять `audioData` с обработанными данными
4. Вызывать `update()` для перерисовки

**Оптимизация**:
- Обрабатывать только сегменты, затронутые перемещением метки
- Использовать throttling для ограничения частоты обработки (например, каждые 50-100ms)
- Кэшировать результаты обработки для одинаковых коэффициентов

```cpp
// В mouseMoveEvent() после обновления позиции метки
if (draggingMarkerIndex >= 0) {
    // Обрабатываем аудио в реальном времени
    processRealtimeStretch();
    scheduleUpdate();
}
```

### 3. Метод обработки в реальном времени

**Новый метод**: `WaveformView::processRealtimeStretch()`

**Важное замечание из исходников**:
- `applyTimeStretchToMarkers()` сортирует метки по `originalPosition` для обработки
- Функция использует `originalPosition` для извлечения сегментов из исходных данных
- Функция использует `position` для вычисления коэффициента растяжения
- После обработки функция возвращает новые метки с обновленными `position` и `originalPosition`
- **НО**: В реальном времени нам нужно сохранить текущие `position` меток (они управляются пользователем)

**Логика**:
1. Использовать `originalAudioData` как источник данных
2. Применить `applyTimeStretchToMarkers()` к исходным данным
3. Обновить `audioData` результатом обработки
4. **НЕ обновлять метки из результата** - сохранить текущие `position` меток
5. НЕ обновлять `originalAudioData` (она остается неизменной до Ctrl+T)

**Важно**: 
- `originalAudioData` НЕ изменяется при перетаскивании меток
- `audioData` обновляется в реальном времени для визуализации
- Текущие `position` меток сохраняются (они управляются пользователем)
- При Ctrl+T `originalAudioData` обновляется на новые обработанные данные

```cpp
void WaveformView::processRealtimeStretch()
{
    if (originalAudioData.isEmpty() || markers.size() < 2) {
        return;
    }
    
    // Сохраняем текущие позиции меток перед обработкой
    QVector<qint64> savedPositions;
    for (const Marker& m : markers) {
        savedPositions.append(m.position);
    }
    
    // Применяем обработку к исходным данным
    MarkerStretchResult result = applyTimeStretchToMarkers(
        originalAudioData,  // Всегда используем исходные данные
        markers,            // Используем текущие метки с их position и originalPosition
        sampleRate
    );
    
    // Обновляем audioData для визуализации
    audioData = result.audioData;
    
    // ВАЖНО: НЕ обновляем метки из result.newMarkers
    // Сохраняем текущие position меток, которые управляются пользователем
    // originalPosition меток остается неизменным в реальном времени
    // (он обновляется только при применении эффекта через Ctrl+T)
}
```

### 4. Проблема с обновлением меток после обработки

**Проблема**: После `applyTimeStretchToMarkers()` возвращаются новые метки с обновленными позициями (`position` и `originalPosition`), но в реальном времени мы хотим сохранить текущие `position` меток, которые задает пользователь.

**Анализ текущей реализации**:
- `applyTimeStretchToMarkers()` обновляет `position` меток на новые значения в обработанном аудио
- Также обновляет `originalPosition = position` (метки "замораживаются" после обработки)
- Это правильное поведение для применения эффекта (Ctrl+T), но не для реального времени

**Решение для реального времени**:
- После `applyTimeStretchToMarkers()` **НЕ использовать** `result.newMarkers`
- Сохранять текущие `position` меток (они управляются пользователем)
- Сохранять текущие `originalPosition` меток (они отражают исходную геометрию)
- Обновлять только `audioData` для визуализации

**Решение для применения эффекта (Ctrl+T)**:
- Использовать `result.newMarkers` полностью
- Обновить `originalAudioData` на новые обработанные данные
- Обновить метки с новыми позициями

**Вывод**: Не нужна модификация `applyTimeStretchToMarkers()` - просто не используем `result.newMarkers` в реальном времени.

### 5. Визуальное отображение измененной волны

**Текущее состояние**: `drawWaveform()` рисует волну из `audioData`

**Что нужно**:
- После `processRealtimeStretch()` `audioData` уже содержит обработанные данные
- `drawWaveform()` автоматически отобразит измененную волну
- Никаких дополнительных изменений в отрисовке не требуется

**Визуальные индикаторы**:
- Сохранить текущие индикаторы (коэффициенты, цветовые метки на ромбиках)
- Добавить визуальное выделение обработанных сегментов (опционально)

### 6. Обновление воспроизведения в реальном времени

**Проблема**: `QMediaPlayer` воспроизводит файл, а не обработанные данные в памяти.

**Варианты решения**:

**Вариант A: Обновление временного файла**
- При каждом изменении метки сохранять обработанное аудио во временный WAV
- Обновлять источник `QMediaPlayer` на новый файл
- **Минусы**: Медленно, много операций ввода-вывода

**Вариант B: Отложенное обновление**
- Обновлять воспроизведение только при отпускании метки (mouseReleaseEvent)
- Во время перетаскивания только визуализация
- **Плюсы**: Быстрее, меньше нагрузки
- **Минусы**: Воспроизведение не синхронизировано с визуализацией

**Вариант C: Гибридный подход** (рекомендуется)
- При перетаскивании: только визуализация измененной волны
- При отпускании метки: обновление временного файла и переключение воспроизведения
- При Ctrl+T: полное применение эффекта с сохранением в основной файл

### 7. Throttling обработки

**Проблема**: Обработка аудио может быть медленной для больших файлов.

**Решение**:
- Использовать `QTimer` для ограничения частоты обработки
- Обрабатывать не чаще чем раз в 50-100ms во время перетаскивания
- Кэшировать последний результат обработки

```cpp
QTimer* realtimeProcessTimer;
bool realtimeProcessPending = false;

void WaveformView::scheduleRealtimeProcess()
{
    if (!realtimeProcessPending) {
        realtimeProcessPending = true;
        QTimer::singleShot(50, this, [this]() {
            if (realtimeProcessPending) {
                realtimeProcessPending = false;
                processRealtimeStretch();
                update();
            }
        });
    }
}
```

### 8. Состояние "режим реального времени"

**Проблема**: Нужно различать режим реального времени (перетаскивание) и режим применения эффекта (Ctrl+T).

**Решение**:
- **НЕ нужен отдельный флаг** `isRealtimeMode`
- Можно определить режим по `draggingMarkerIndex >= 0` (метка перетаскивается)
- В `processRealtimeStretch()` всегда используем `originalAudioData` и не обновляем метки
- В `applyTimeStretch()` (через Ctrl+T) обновляем и `audioData`, и `originalAudioData`, и метки

**Логика**:
- **Реальный режим** (перетаскивание): 
  - `audioData` обновляется из `originalAudioData` с текущими метками
  - `originalAudioData` НЕ изменяется
  - Метки НЕ обновляются (сохраняются текущие `position` и `originalPosition`)
- **Режим применения** (Ctrl+T):
  - `audioData` обновляется на новые обработанные данные
  - `originalAudioData` обновляется на новые обработанные данные (становятся новыми "исходными")
  - Метки обновляются с новыми позициями (`position` и `originalPosition` синхронизируются)

## Этапы реализации

### ✅ Этап 1: Кэширование исходных данных (ЗАВЕРШЕН)
1. ✅ Добавлено `originalAudioData` в `WaveformView` (в private секцию)
2. ✅ Обновлен `setAudioData()` для сохранения исходных данных:
   - Сохраняет `originalAudioData = audioData` после нормализации
   - Происходит при первой загрузке файла
3. ✅ Обновлен `applyTimeStretch()` в `MainWindow`:
   - Обновление `originalAudioData` происходит через `TimeStretchCommand::redo()`
   - Новые обработанные данные становятся "исходными" для следующих операций
4. ✅ Обновлены `TimeStretchCommand::redo()` и `undo()`:
   - В `redo()`: обновляет `originalAudioData` на `newAudioData`
   - В `undo()`: восстанавливает `originalAudioData` из `oldAudioData`
   - Обеспечивает корректную работу undo/redo в реальном времени

### ✅ Этап 2: Метод обработки в реальном времени (ЗАВЕРШЕН)
1. ✅ Создан `processRealtimeStretch()`
2. ✅ Реализована логика обработки на основе `originalAudioData`
3. ✅ Обновляет только `audioData`, сохраняя `originalAudioData`
4. ✅ Метки НЕ обновляются (сохраняются текущие `position` и `originalPosition`)

### ✅ Этап 3: Интеграция с перетаскиванием (ЗАВЕРШЕН)
1. ✅ Добавлен вызов `scheduleRealtimeProcess()` в `mouseMoveEvent()` после обновления позиции метки
2. ✅ Реализован `scheduleRealtimeProcess()` с throttling (50ms)
3. ✅ Реализован `processRealtimeStretch()` для обработки в реальном времени
4. ✅ Метки НЕ обновляются в реальном времени (сохраняются текущие `position`)

### ✅ Этап 4: Обновление меток после обработки (ЗАВЕРШЕН)
1. ✅ В `processRealtimeStretch()` НЕ используется `result.newMarkers`
2. ✅ Сохраняются текущие `position` и `originalPosition` меток
3. ✅ При применении эффекта (Ctrl+T) используется `result.newMarkers` полностью

### ✅ Этап 5: Обновление воспроизведения (ЗАВЕРШЕН)
1. ✅ Реализовано обновление временного файла при отпускании метки
2. ✅ Реализован сигнал `markerDragFinished()` в `WaveformView`
3. ✅ Реализован метод `updatePlaybackAfterMarkerDrag()` в `MainWindow`
4. ✅ Обновляется `QMediaPlayer` на новый файл с сохранением позиции воспроизведения
5. ✅ Позиция воспроизведения масштабируется пропорционально новой длине файла

### ⚠️ Этап 6: Оптимизация и тестирование (В ПРОЦЕССЕ)
1. ⚠️ Оптимизация обработки для больших файлов (может потребоваться дополнительная оптимизация)
2. ⚠️ Добавление прогресс-бара для длительных операций (опционально)
3. ⚠️ Тестирование на различных файлах (требуется пользовательское тестирование)

## Технические детали

### Структура данных

```cpp
class WaveformView {
    QVector<QVector<float>> audioData;        // Текущие данные для визуализации
    QVector<QVector<float>> originalAudioData; // Исходные данные для пересчета
    
    // Оптимизация: throttling для реального времени
    QTimer* realtimeProcessTimer;             // Таймер для throttling (можно использовать существующий updateTimer)
    bool realtimeProcessPending;              // Флаг ожидания обработки
    
    // Примечание: isRealtimeMode не нужен отдельно - 
    // можно определить по draggingMarkerIndex >= 0
};
```

**Важно**: 
- `originalAudioData` инициализируется при `setAudioData()`
- Обновляется только при применении эффекта через `updateOriginalData()`
- В реальном времени используется только для чтения

### Методы

```cpp
// Обработка в реальном времени
void processRealtimeStretch();

// Планирование обработки с throttling
void scheduleRealtimeProcess();

// Обновление исходных данных после применения эффекта
void updateOriginalData(const QVector<QVector<float>>& newData);
```

**Детали реализации**:

```cpp
void WaveformView::scheduleRealtimeProcess()
{
    if (!realtimeProcessPending) {
        realtimeProcessPending = true;
        // Используем существующий updateTimer или создаем новый
        QTimer::singleShot(50, this, [this]() {
            if (realtimeProcessPending) {
                realtimeProcessPending = false;
                processRealtimeStretch();
                scheduleUpdate(); // Используем существующий throttled update
            }
        });
    }
}

void WaveformView::processRealtimeStretch()
{
    if (originalAudioData.isEmpty() || markers.size() < 2) {
        return;
    }
    
    // Применяем обработку к исходным данным
    MarkerStretchResult result = applyTimeStretchToMarkers(
        originalAudioData,  // Всегда исходные данные
        markers,            // Текущие метки с их position и originalPosition
        sampleRate
    );
    
    // Обновляем только audioData для визуализации
    audioData = result.audioData;
    
    // НЕ обновляем метки - сохраняем текущие position и originalPosition
    // Они управляются пользователем и отражают исходную геометрию
}

void WaveformView::updateOriginalData(const QVector<QVector<float>>& newData)
{
    originalAudioData = newData;
}
```

### Изменения в существующих методах

```cpp
// setAudioData() - сохранять исходные данные
void WaveformView::setAudioData(const QVector<QVector<float>>& data)
{
    // Нормализация данных (существующий код)
    audioData.clear();
    // ... нормализация ...
    
    // ВАЖНО: Сохраняем исходные данные ДО нормализации
    // Но на самом деле нормализация не изменяет форму волны,
    // только масштаб, поэтому можно сохранить после нормализации
    originalAudioData = audioData; // Сохраняем нормализованные данные как исходные
    
    // ... остальной код ...
}

// НОВЫЙ метод: обновление исходных данных после применения эффекта
void WaveformView::updateOriginalData(const QVector<QVector<float>>& newData)
{
    originalAudioData = newData;
}

// applyTimeStretch() в MainWindow - обновлять originalAudioData после применения
void MainWindow::applyTimeStretch()
{
    // Получаем текущие данные и метки
    QVector<QVector<float>> oldData = waveformView->getAudioData();
    QVector<Marker> currentMarkers = waveformView->getMarkers();
    
    // Применяем обработку
    WaveformView::TimeStretchResult result = waveformView->applyTimeStretch(currentMarkers);
    QVector<QVector<float>> newData = result.audioData;
    QVector<Marker> newMarkers = result.newMarkers;
    
    // Обновляем данные и метки
    waveformView->setAudioData(newData);
    waveformView->setMarkers(newMarkers);
    
    // ВАЖНО: Обновляем исходные данные на новые обработанные данные
    // Теперь новые данные становятся "исходными" для следующих операций
    waveformView->updateOriginalData(newData);
    
    // ... остальной код (undo/redo, воспроизведение) ...
}

// ВАЖНО: Обновить TimeStretchCommand для работы с originalAudioData
// В markerstretchengine.cpp:

void TimeStretchCommand::redo()
{
    if (waveformView) {
        waveformView->setAudioData(newAudioData);
        waveformView->setMarkers(newMarkers);
        // ВАЖНО: Обновляем originalAudioData на новые данные
        waveformView->updateOriginalData(newAudioData);
    }
}

void TimeStretchCommand::undo()
{
    if (waveformView) {
        waveformView->setAudioData(oldAudioData);
        waveformView->setMarkers(oldMarkers);
        // ВАЖНО: Восстанавливаем originalAudioData из старых данных
        waveformView->updateOriginalData(oldAudioData);
    }
}

// mouseMoveEvent() - обрабатывать в реальном времени
void WaveformView::mouseMoveEvent(QMouseEvent* event)
{
    if (draggingMarkerIndex >= 0 && (event->buttons() & Qt::LeftButton)) {
        // ... существующий код обновления позиции метки ...
        
        // После обновления позиции метки:
        scheduleRealtimeProcess(); // Планируем обработку с throttling
        scheduleUpdate(); // Планируем перерисовку
    }
}

// mouseReleaseEvent() - опционально обновить воспроизведение
void WaveformView::mouseReleaseEvent(QMouseEvent* event)
{
    if (event->button() == Qt::LeftButton && draggingMarkerIndex >= 0) {
        // Завершили перетаскивание метки
        draggingMarkerIndex = -1;
        
        // Опционально: обновить воспроизведение здесь
        // (см. раздел "Обновление воспроизведения")
    }
}
```

## Потенциальные проблемы и решения

### Проблема 1: Производительность
**Симптом**: Задержки при перетаскивании меток
**Решение**: 
- Throttling обработки
- Обработка только измененных сегментов
- Кэширование результатов

### Проблема 2: Память
**Симптом**: Увеличение потребления памяти
**Решение**:
- `originalAudioData` занимает дополнительную память, но это необходимо
- Можно использовать сжатие или lazy loading для больших файлов

### Проблема 3: Синхронизация меток
**Симптом**: Позиции меток изменяются после обработки
**Решение**:
- Сохранять текущие `position` меток
- Обновлять только `originalPosition` при необходимости

### Проблема 4: Отмена изменений
**Симптом**: Как отменить изменения в реальном времени?
**Решение**:
- **Вариант A**: При отпускании метки без применения эффекта восстанавливать `audioData` из `originalAudioData`
  - Простое решение, но теряются изменения при отпускании
- **Вариант B**: Использовать систему команд для отмены перетаскивания
  - Более сложно, но сохраняет возможность отмены
- **Вариант C** (рекомендуется): Не отменять автоматически, но добавить кнопку "Отменить изменения меток"
  - Пользователь сам решает, применять изменения или нет
  - Можно реализовать через восстановление `audioData` из `originalAudioData` и сброс `position` меток на `originalPosition`

**Важно для undo/redo**:
- `TimeStretchCommand` должен обновлять `originalAudioData` при `redo()` и `undo()`
- Это обеспечит корректную работу реального времени после undo/redo

## Альтернативные подходы

### Подход A: Полная обработка при каждом движении
- Простота реализации
- Медленно для больших файлов
- Высокая нагрузка на CPU

### Подход B: Обработка только визуализации
- Быстро
- Воспроизведение не синхронизировано
- Нужна отдельная обработка для воспроизведения

### Подход C: Гибридный (рекомендуется)
- Баланс между производительностью и функциональностью
- Визуализация в реальном времени
- Воспроизведение обновляется при отпускании метки

## Критерии успеха

1. ✅ **При перетаскивании метки волна визуально изменяется между метками** - РЕАЛИЗОВАНО
2. ✅ **Коэффициенты отображаются корректно** - РЕАЛИЗОВАНО
3. ✅ **Обработка происходит с тонкомпенсацией** - РЕАЛИЗОВАНО
4. ✅ **Производительность достаточна для плавной работы** - РЕАЛИЗОВАНО (throttling 50ms)
5. ✅ **При применении эффекта (Ctrl+T) изменения сохраняются** - РЕАЛИЗОВАНО
6. ✅ **Система отмены работает корректно** - РЕАЛИЗОВАНО
7. ✅ **Воспроизведение обновляется при отпускании метки** - РЕАЛИЗОВАНО
8. ✅ **Позиция воспроизведения сохраняется при обновлении** - РЕАЛИЗОВАНО

## Статус реализации

### ✅ Реализовано (100%)

1. ✅ **Кэширование исходных данных** - `originalAudioData` хранит исходные данные
2. ✅ **Базовая обработка в реальном времени** - `processRealtimeStretch()` работает при перетаскивании
3. ✅ **Throttling** - `scheduleRealtimeProcess()` ограничивает частоту обработки до 50ms
4. ✅ **Обновление воспроизведения** - `updatePlaybackAfterMarkerDrag()` обновляет QMediaPlayer при отпускании метки
5. ✅ **Синхронизация с undo/redo** - `TimeStretchCommand` корректно работает с `originalAudioData`

### 📝 Детали реализации

**Добавленные поля в WaveformView:**
- `QVector<QVector<float>> originalAudioData` - исходные данные для пересчета
- `bool realtimeProcessPending` - флаг ожидания обработки в реальном времени

**Добавленные методы:**
- `void processRealtimeStretch()` - обработка аудио в реальном времени
- `void scheduleRealtimeProcess()` - планирование обработки с throttling
- `void updateOriginalData(const QVector<QVector<float>>& newData)` - обновление исходных данных

**Добавленные сигналы:**
- `void markerDragFinished()` - сигнал о завершении перетаскивания метки

**Изменения в существующих методах:**
- `setAudioData()` - сохраняет исходные данные в `originalAudioData`
- `mouseMoveEvent()` - вызывает `scheduleRealtimeProcess()` при перетаскивании метки
- `mouseReleaseEvent()` - отправляет сигнал `markerDragFinished()` при отпускании метки
- `TimeStretchCommand::redo()` - обновляет `originalAudioData`
- `TimeStretchCommand::undo()` - восстанавливает `originalAudioData`

**Новые методы в MainWindow:**
- `void updatePlaybackAfterMarkerDrag()` - обновляет воспроизведение после перетаскивания метки

### ⚠️ Возможные улучшения (опционально)

1. **Оптимизация для больших файлов**: 
   - Обработка только измененных сегментов вместо всего аудио
   - Кэширование результатов обработки для одинаковых коэффициентов
   
2. **Прогресс-бар**: 
   - Показывать прогресс при обработке больших файлов
   
3. **Более точное сохранение позиции воспроизведения**:
   - Пересчет позиции по меткам вместо пропорционального масштабирования

---

## Итоги реализации

### ✅ Реализованный функционал

**Основные возможности:**
1. ✅ **Визуальная обратная связь в реальном времени**: При перетаскивании метки волна визуально изменяется между метками с отображением коэффициентов сжатия/растяжения
2. ✅ **Обработка с тонкомпенсацией**: Все изменения применяются с сохранением высоты тона через алгоритм overlap-add
3. ✅ **Обновление воспроизведения**: При отпускании метки воспроизведение автоматически обновляется на обработанное аудио
4. ✅ **Сохранение позиции**: Позиция воспроизведения сохраняется и масштабируется пропорционально новой длине файла
5. ✅ **Интеграция с undo/redo**: Система отмены/повтора корректно работает с реальным временем

**Технические детали:**
- Throttling обработки: 50ms для плавной работы
- Кэширование исходных данных: `originalAudioData` для корректного пересчета
- Оптимизация производительности: throttled updates для UI

### 📊 Статистика реализации

- **Добавлено полей**: 2 (`originalAudioData`, `realtimeProcessPending`)
- **Добавлено методов**: 3 (`processRealtimeStretch()`, `scheduleRealtimeProcess()`, `updateOriginalData()`)
- **Добавлено сигналов**: 1 (`markerDragFinished()`)
- **Изменено методов**: 5 (`setAudioData()`, `mouseMoveEvent()`, `mouseReleaseEvent()`, `TimeStretchCommand::redo()`, `TimeStretchCommand::undo()`)
- **Новых методов в MainWindow**: 1 (`updatePlaybackAfterMarkerDrag()`)

### 🎯 Результат

Метки сжатия/растяжения теперь работают полностью интерактивно:
- Пользователь видит изменения волны в реальном времени при перетаскивании
- Воспроизведение автоматически обновляется при отпускании метки
- Все изменения применяются с тонкомпенсацией
- Система отмены/повтора работает корректно

**Дата реализации**: 2024

